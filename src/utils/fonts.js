import { Overpass,Convergence } from 'next/font/google';

 
export const font_overpass = Overpass({ subsets: ['latin'] });
export const font_convergence = Convergence({ subsets: ['latin'],weight:'400' });
 